package atlas

trait ValueEncoderBoilerplate {
  self: ValueEncoderFunctions =>

  [#implicit def function1[[#A1#], R](
    implicit
    [#argType1: TypeEncoder[A1],
    argDec1: ValueDecoder[A1]#,
    ],
    resType: TypeEncoder[R],
    resEnc: ValueEncoder[R]
  ): ValueEncoder[([#A1#]) => R] =
    pure(func => Value.native(func))
  #

  ]
}
